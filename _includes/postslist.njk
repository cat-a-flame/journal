<ol reversed class="postlist">
    {% for post in postslist | reverse %}
        <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
            
            {% if post.data.image %}
                <div>
                    <img class="postlist-image" src="{{ post.data.image }}" alt="" />
                </div>
            {% endif %}

            <div class="postlist-details">
                <div class="postlist-meta">
                    <!--a href="/collections/{{ post.data.categories | safe }}" class="postlist-category">{{ post.data.categories | safe }}</a-->
                    <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("yyyy LLLL dd.") }}</time>
                </div>
                
                <a href="{{ post.url }}" class="postlist-link">{{ post.data.title }}</a>

                {% if post.data.lead %}
                    <p class="postlist-lead">
                        {{ post.data.lead | safe }}
                    </p>

                    <!--a class="read-more-link" href="{{ post.url }}">Read more <span aria-hidden="true">→</span></a-->
                {% endif %}
            </div>
        </li>
    {% endfor %}
</ol>